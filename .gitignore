# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Dependencies
node_modules/
package-lock.json
yarn.lock
.superpowers/

# Secrets — the OpenAI key for /api/ai-review and the WCL_CLIENT_ID / WCL_CLIENT_SECRET
# credentials for the Warcraft Logs proxy live here, never in the repo
.env

# Calibration harness: the pinned wowsims checkout, its built binary and every sim
# artefact are reproducible from links.txt + the SHA in calibration/README.md.
# weights.json and floors-report.md ARE committed — they are the evidence behind the
# engine's numbers.
calibration/vendor/
calibration/out/
calibration/dump-profiles/out/
scratchpad/
